# 2차 재검증 AI 서버

## 1. 개요

### 1.1 배경 및 목적

엣지 디바이스에서 YOLOv8s 모델로 포트홀을 탐지하는 과정에서 다음과 같은 오탐 문제가 확인되었다.

- **위치 기반 오탐**: 나무, 하늘 등 도로와 무관한 영역에서 포트홀로 잘못 탐지
- **형태·색상 기반 오탐**: 맨홀 뚜껑, 차량 대시보드, 그림자, 물웅덩이 등 포트홀과 외관이 유사한 객체에 대한 오탐

이를 해결하기 위해 엣지 탐지 이후 서버 단에서 최종 판단을 수행하는 2단계 파이프라인을 도입한다.

### 1.2 시스템 구조 요약

```
[엣지 디바이스]                    [2차 재검증 서버]
YOLO 모델로 포트홀 후보 탐지  →  전처리 + 분류 모델로 최종 판정
(후보 이미지 크롭 전송)            (True Pothole / False Positive)
```

---

## 2. 입출력 정의

### 2.1 입력

- **형태**: 엣지 디바이스의 1차 YOLO 모델이 포트홀로 판단한 영역의 크롭(crop) 이미지
- **포함 정보**: 크롭 이미지, 바운딩 박스 좌표, confidence score, 촬영 시각, 위치(GPS)

### 2.2 출력

- **판정 결과**: 포트홀 확정 / False Positive 폐기
- **부가 정보**: 분류 클래스, confidence score
- **저장 대상**: 포트홀 확정 케이스만 DB에 기록

---

## 3. 아키텍처 설계

### 3.1 파이프라인 흐름

```
1. 크롭 이미지 수신
        ↓
2. 전처리
   - 리사이즈 및 정규화
   - 야간·저조도 보정 (Histogram Equalization, Gamma Correction)
   - 이미지 품질 필터링 (블러·노이즈 심한 이미지 자동 폐기)
        ↓
3. 분류 모델 추론
   - 다중 클래스 분류
        ↓
4. Confidence Threshold 판단
   - 기준 이상: 포트홀 확정 → DB 저장
   - 기준 미만: False Positive → 폐기
        ↓
5. 결과 반환
```

### 3.2 핵심 설계 원칙

**탐지(Detection) 모델이 아닌 분류(Classification) 모델을 사용하는 이유**

1차 YOLO 모델이 이미 위치 탐지를 완료한 상태에서 크롭 이미지가 입력된다. 2차 서버의 핵심 임무는 "이 크롭이 진짜 포트홀이냐?"이므로 이는 객체 탐지가 아닌 이진/다중 분류 문제에 해당한다. 탐지 모델(YOLO, RT-DETR)을 2차에 적용하면 이미 완료된 위치 탐지 연산이 중복 수행되어 낭비가 발생한다.

분류 모델 적용 시 장점은 다음과 같다.

- 탐지(1차)와 검증(2차)의 역할 명확히 분리
- 크롭 입력에 최적화된 구조
- CPU 운영 가능 → 온디맨드 GPU 전략과 적합
- 응답 속도 빠름, 운영 비용 낮음
- YOLO와 다른 계열 모델 사용으로 오탐 패턴 상호 보완

---

## 4. 모델 선택

### 4.1 분류 모델 비교

|모델|정확도|CPU 가능|파라미터|추천 상황|
|---|---|---|---|---|
|EfficientNet-B0|보통|✅ 가능|5.3M|CPU 상시 운영, 비용 최소화|
|EfficientNet-B4|높음|🔺 느림|19M|정확도·속도 균형|
|ConvNeXt-Tiny|높음|✅ 가능|28M|최신 CNN, 안정적|
|ConvNeXt-S|매우 높음|🔺 느림|50M|GPU 여유 시 정확도 우선|
|ResNet-50|보통|✅ 가능|25M|논문 비교 기준점|

**권장**: 온디맨드 GPU 환경을 고려하여 GPU 가동 시 EfficientNet-B4 / ConvNeXt-S, CPU 운영 시 EfficientNet-B0 / ConvNeXt-Tiny로 전환하는 이중 전략을 채택한다. ONNX 변환을 통해 동일한 모델 파일로 GPU/CPU 전환이 가능하도록 설계한다.

### 4.2 분류 클래스 정의

단순 이진 분류(포트홀/비포트홀)보다 오탐 유형을 세분화한 다중 클래스 분류를 적용하면 Hard Negative 학습 효과가 높아진다.

|클래스|설명|
|---|---|
|`pothole`|실제 포트홀|
|`manhole`|맨홀 뚜껑|
|`shadow`|그림자|
|`crack`|도로 균열 (포트홀 아님)|
|`puddle`|물웅덩이|
|`patch`|도로 보수 흔적|
|`road_stain`|도로 얼룩·마킹|
|`normal_road`|일반 도로|

### 4.3 다른 아키텍처 선택지 비교

|옵션|2차 모델|장점|단점|추천 상황|
|---|---|---|---|---|
|①|YOLO26 / YOLO26-seg|동일 생태계, 빠른 적용, NMS-free, CPU 43% 빠름|1차와 오탐 패턴 공유 가능성|빠른 프로토타입|
|②|RT-DETR-X|AP 54.8 최고 정확도, Transformer 전역 attention|GPU 의존, 파인튜닝 난이도 높음|정확도 최우선, 연구 목적|
|③|SAM2 + 탐지 모델|픽셀 마스크 추출, 면적·심각도 동시 분석 가능|무거움, 파이프라인 복잡|면적·심각도 분석까지 필요 시|
|④|앙상블 (다중 모델)|이론적 최고 정확도, 오탐 패턴 상호 보완|비용·지연 가장 큼|연구 목적|
|⑤|**분류 모델** ★|역할 분리 명확, 크롭 최적화, CPU 가능, 비용 낮음|면적·위치 정보 없음|**현 단계 최적**|

---

## 5. 학습 데이터 전략

### 5.1 데이터 수집

- 실제 포트홀 현장 이미지
- Hard Negative 데이터: 맨홀, 그림자, 물웅덩이, 균열, 도로 보수 흔적 등 오탐 유형별 적극 수집
- 생성형 AI(Midjourney, Stable Diffusion 등)를 활용한 합성 포트홀 이미지 보강  
    → 관련 연구에서 실제 이미지 대비 성능 차이가 크지 않음이 확인됨

### 5.2 데이터 증강

기존 크롭, 블러 추가 외에 다음 기법을 추가 적용한다.

- 색상 변환 (Color Jitter, 채도·명도 조정)
- 흑백 변환
- 초고해상도 변환 (ESRGAN 등 Super Resolution 모델 활용)
- 야간·저조도 시뮬레이션
- 강수·안개 효과 합성
- Histogram Equalization, Gamma Correction 적용

### 5.3 SAM2 활용 자동 라벨링

기존 바운딩 박스 라벨을 SAM2에 프롬프트로 입력하여 픽셀 단위 마스크를 자동 생성한다. 별도의 마스크 라벨링 작업 없이 세그멘테이션 수준의 학습 데이터를 확보할 수 있어 라벨링 비용을 크게 절감할 수 있다.

---

## 6. 배포 환경 및 인프라

### 6.1 GPU 운영 전략

|상황|운영 방식|모델|
|---|---|---|
|평시|CPU 상시 운영|EfficientNet-B0 / ConvNeXt-Tiny|
|요청 급증 시|GPU 온디맨드 가동|EfficientNet-B4 / ConvNeXt-S|

GPU 온디맨드 트리거 조건은 요청 큐 크기, 시간대, 처리 지연 등을 기준으로 별도 정의가 필요하다.

### 6.2 모델 호환성

ONNX 형식으로 변환하여 GPU/CPU 환경 전환 시 동일한 모델 파일을 사용한다. 이를 통해 환경에 따른 코드 변경 없이 런타임만 교체 가능하도록 설계한다.

### 6.3 서버 장애 대응

2차 서버 장애 시 fallback 전략을 사전에 정의해야 한다.

- **옵션 A**: 1차 YOLO 결과를 그대로 사용 (오탐 감수)
- **옵션 B**: 재검증 보류 후 복구 시 일괄 처리

---

## 7. 성능 평가 기준

### 7.1 핵심 지표

- **Precision**: 포트홀로 판정한 것 중 실제 포트홀 비율 → False Positive 제거가 목적이므로 우선 지표
- **Recall**: 실제 포트홀 중 탐지한 비율 → 놓치는 케이스 모니터링
- **mAP[.5:.95]**: 전체 탐지 성능 종합 지표

### 7.2 Confidence Threshold 설정

False Positive 제거가 목적이므로 threshold를 높게 설정한다(권장 0.7~0.8 이상). 실제 값은 Validation Set의 Precision-Recall 곡선을 분석하여 결정한다.

---

## 8. 미결 사항 (TODO)

|항목|내용|우선순위|
|---|---|---|
|전송 방식|엣지→서버: 동기(API) vs 비동기(메시지 큐) 결정|높음|
|SLA 정의|재검증 응답 시간 기준 정의|높음|
|GPU 트리거|온디맨드 가동 조건 구체화|중간|
|개인정보 처리|이미지 내 번호판·사람 얼굴 blur 처리 정책|중간|
|모델 버전 관리|재학습 주기, 롤백 전략|중간|
|모니터링|배포 후 정확도 저하 감지 방법|중간|
|DB 설계|저장 항목 정의 (위치, 시각, confidence, 이미지 등)|낮음|

---

## 9. 향후 확장 계획 (중장기 로드맵)

현재는 False Positive 제거를 목적으로 분류 모델 기반의 단순한 구조로 시작한다. 이후 운영 안정화 이후 다음 방향으로 단계적 확장을 검토한다.

**단기**: 엣지 모델 고도화 (YOLO26, 증강 데이터 학습) → 서버 의존도 감소

**중장기**: 재검증 서버를 고부가가치 분석 플랫폼으로 발전

- SAM2를 통한 포트홀 면적 정밀 측정
- 심각도 자동 분류 및 보수 우선순위 산정
- LLM을 활용한 원인 분석 및 유지보수 리포트 자동 생성

단, 이 방향으로 나아가기 위해서는 상당한 개발 비용과 시간이 소요되며, 충분한 실험적 검증과 자원 확보가 선행되어야 한다. 따라서 현재는 추후 과제로 남겨두고 단계적으로 도입 여부를 검토한다.